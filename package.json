{
  "name": "toucan",
  "version": "0.1.0",
  "description": "TUI dashboard for AI token usage — scans Claude Code, Codex CLI, and Gemini CLI local data",
  "type": "module",
  "bin": {
    "toucan": "src/main.ts"
  },
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun --watch run src/main.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/node": "^25.3.0"
  }
}
